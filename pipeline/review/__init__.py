"""Deterministic static review of a git diff.

Standard-library only and instant, so it stays usable as a pre-commit hook.
The model-augmented workflow in flow.py builds on top of it rather than
replacing it."""
__version__ = "0.1.0"
